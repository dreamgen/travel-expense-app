# 雲端資料同步範圍與角色權限盤點規格書

## 1. 資料範圍盤點 (Data Scope Inventory)

系統資料被劃分為以下五個獨立的邏輯區塊（Scope），同步時應分別處理：

| 資料區塊 (Scope) | 內容描述 | 儲存位置 (Google Sheet) | 唯一識別鍵 (Key) |
| :--- | :--- | :--- | :--- |
| **A. 全域行程資訊 (Global Config)** | 行程名稱、旅遊日期、基礎貨幣 | Config / Settings | Key (固定欄位) |
| **B. 匯率設定 (Exchange Rates)** | 各國貨幣對台幣的匯率 | Config / Settings | CurrencyCode |
| **C. 成員名單 (Member Directory)** | 所有成員的 UID、姓名、員工編號、權限 | Members | UID |
| **D. 個人記帳內容 (Expense Details)** | 消費日期、項目、金額、幣別、分類 | Expenses (前段欄位) | UID + Timestamp (或 UUID) |
| **E. 審核資訊 (Audit Info)** | 審核狀態 (待審/通過/駁回)、審核備註 | Expenses (後段欄位) | 同上 (依附於單據) |

## 2. 角色權限矩陣 (Role Permission Matrix)

定義各角色對上述資料區塊的操作權限。

| 角色 | A. 全域行程 | B. 匯率設定 | C. 成員名單 | D. 個人記帳內容 | E. 審核資訊 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **團員 (Member)** | 讀取 | 讀取 | 僅更新自己 | 僅讀寫自己 | 僅讀取 (系統自動重置) |
| **團長 (Admin) - App端** | 讀寫 | 讀寫 | 僅更新自己 | 僅讀寫自己 | 僅讀取 (系統自動重置) |
| **團長 (Admin) - 後台端** | 讀寫 | 讀寫 | 管理全員 | 特權修正 (含註記) | 僅讀取 |
| **審核人員 (Auditor)** | 讀取 | 讀取 | 讀取 | 讀取 | **寫入 (Write)** |

### 權限說明：

*   **審核人員**：擁有全系統資料的讀取權，但唯一能寫入的只有 **E. 審核資訊**。
*   **審核資訊保護**：團員或團長在更新自己的單據時，不能直接修改審核狀態（例如不能自己把自己改成「通過」）。

## 3. 雲端更新邏輯與邊界 (Cloud Update Logic & Boundaries)

此章節定義後端 API (`doPost`) 接收到同步請求時的處理邏輯。

### 3.1 邏輯一：團員/團長(App) 個人同步 (Member Sync)

*   **情境**：團員上傳自己的新單據或修改舊單據。
*   **輸入資料**：`{ uid, role: "member", expenses: [ {id: "exp1", amount: 100, item: "Dinner"...} ] }`

**處理規則：**

| 資料區塊 | 處理動作 (Action) | 邏輯細節 |
| :--- | :--- | :--- |
| **D. 個人記帳內容** | 替換 (Replace) | 刪除該 UID 舊資料，寫入新資料。 |
| **E. 審核資訊** | 重置或保留 (Reset/Keep) | 1. 後端比對新舊資料。<br>2. 若單據內容(金額/日期)有變更：系統強制將狀態重置為「**待審核 (Pending)**」，並清空備註。<br>3. 若為新單據：預設為「待審核」。<br>4. 嚴禁直接採信前端上傳的 `status` 欄位值。 |

### 3.2 邏輯二：團長(App) 全域同步 (Admin Global Sync)

*   **情境**：團長在 App 修改行程/匯率。
*   **處理規則**：同原規格，更新 A, B, C(自己)，D(自己)，E(重置/保留)。

### 3.3 邏輯三：後台管理介面 - 成員/單據修正 (Admin Console)

*   **情境**：團長修正成員或單據。
*   **處理規則**：同原規格。若修正單據內容，通常也會連帶將審核狀態重置為「待審核」或由團長註記說明。

### 3.4 邏輯四：審核人員同步 (Auditor Sync)

*   **情境**：審核人員在 App 或後台介面，對多筆單據進行「通過」或「駁回」並填寫備註。
*   **輸入資料**：`{ uid: "auditor_01", role: "auditor", audit_updates: [ { id: "exp_01", status: "Approved", remark: "OK" }, { id: "exp_02", status: "Rejected", remark: "No receipt" } ] }`

**處理規則：**

| 資料區塊 | 處理動作 (Action) | 邏輯細節 |
| :--- | :--- | :--- |
| **A, B, C** | 忽略 (Ignore) | 無權限。 |
| **D. 個人記帳內容** | 唯讀 (Read-Only) | 嚴禁修改消費金額、日期、項目等原始資料。 |
| **E. 審核資訊** | 更新指定欄位 (Update Fields) | 1. 根據 `id` 搜尋對應的單據列。<br>2. 僅更新該列的 `Status` 與 `Remark` 欄位。<br>3. 若找不到單據 ID，回傳錯誤或忽略。 |

## 4. 衝突與例外處理 (Conflict Resolution)

**Q: 審核人員正在審核，團員同時修改了單據，怎麼辦？**

*   **情境**：
    *   審核人員讀取單據 A (金額 100)，按下「通過」。
    *   團員同時發現打錯了，改成金額 200，並同步上傳。

*   **衝突結果**：
    *   **若 團員先 同步成功**：金額變 200，狀態變「待審核」。審核人員稍後送出「通過」指令，若後端檢查 ID 存在，可能會錯誤地將 200 元的單據標為通過（視實作而定）。
    *   **若 審核人員先 同步成功**：單據變「通過」。團員稍後同步金額 200，後端偵測到內容變更，強制將狀態重置為「待審核」。

*   **解決方案 (Backend Logic)**：
    1.  **版本檢查 (Version Check)**：最佳解。審核人員送出時帶上 `last_updated_timestamp`。若後端發現單據已被團員更新（時間戳較新），則拒絕審核寫入，提示「資料已變更，請重新整理」。
    2.  **簡易解 (Reset Priority)**：只要團員有更新內容，一律重置為「待審核」。這確保了審核過的永遠是「當時看到的那個版本」。

## 5. 總結：後端開發檢核 (Backend Checklist)

- [ ] **審核人員 API**：新增專門處理 `audit_updates` 的邏輯分支。
- [ ] **欄位保護**：
    - `doPost` 處理 `role: member` 時，忽略 Payload 中的 `status` / `remark`，改由後端邏輯決定（新單據=待審，修改單據=重置待審）。
    - `doPost` 處理 `role: auditor` 時，只准許 `update` 對應的欄位索引，不准許 `deleteRow` 或 `setValues` 整列。