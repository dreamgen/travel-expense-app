# 員工清單使用情境說明與調整

## 需求說明

Employee 的資料如附圖，用途只有在申請單及統計員工可補助金額與已補助的身份辨識與統計。
而 Member 才是系統用來辨識使用者的欄位，因此在操作及權限上仍以 Member 的名稱為 Key 值來執行。
但最終申請單必須對應到員工才能統計與判斷是否可補助。

### 流程如下：

1. **團長初始旅程**：團長初始一個 Tripcode 的旅程時必須選擇員工對應代表團長的身份，並記錄 Member 與員工的關連（`Tripcode` + `MemberName` + `EmployeeID`）。
2. **新增團員**：團長可在設定介面新增團員，但必須由員工清單中選取加入。
3. **團員加入/恢復**：團員在收到邀請 URL 或是自行登入 TripCode 後，必須選擇對應的團員（Email 比對，或是選擇已加入的團員員工姓名（員工姓名 + MemberName）），選擇後若此團員已有單據資料代表此裝置要恢復此團員的資料。
4. **新團員註冊**：若是新加入團員，則使用登入時輸入的姓名為 MemberName，而選取對應的員工姓名（可用 Email 或是真實姓名快速篩選下拉選單）。
5. **後續操作**：後續操作皆使用 MemberName 即可，員工姓名已存在關聯。
6. **統計與產表**：當完成審核要產出申請表時，及後台管理的員工清單統計時，則必須關聯到員工來進行申請表或是統計。

---

## 1. 核心邏輯變更摘要

本方案根據最新的業務邏輯進行調整：

*   **移除全域 Member 概念**：改為 `TripMembers`，Member 身份僅在單一 Tripcode 內有效。
*   **裝置重置機制**：輸入 Tripcode 即視為切換/重置帳號，強制從雲端下載最新資料並覆蓋本機資料。
*   **強制線上登入**：首次進入 Tripcode 必須連網，建立本機資料庫後才允許離線操作。
*   **身份與統計分離**：前端操作顯示 `MemberName`，後端統計綁定 `EmployeeID`。

---

## 2. 資料庫架構設計 (Google Sheets)

### 2.1 新增/調整工作表

#### A. Employees (員工主檔 - 維持現狀)
*   **用途**：靜態的人事資料，供選單使用。
*   **欄位**：
    *   `EmployeeID` (Key)
    *   `Name`
    *   `Email` (Unique)
    *   `Department`
    *   `MonthlyLimit` (補助上限)
    *   `UsedAmount` (已用額度)

#### B. TripMembers (旅程成員關聯表 - 新核心)
*   **用途**：記錄某個旅程中，誰 (Member) 代表誰 (Employee)。
*   **欄位**：
    *   `Tripcode` (Key 1)
    *   `MemberName` (Key 2 - Trip 內唯一)
    *   `EmployeeID` (關聯 Employee)
    *   `Role` (Leader/Member)
    *   `Status` (Active/Inactive)
    *   `JoinDate` (Timestamp)

#### C. Expenses (支出表 - 調整欄位)
*   **新增欄位**：除了 `MemberName` 外，必須寫入 `EmployeeID`。
*   **欄位順序建議**：`ID`, `Tripcode`, `Date`, `Item`, `Amount`, `MemberName`, `EmployeeID`, `Category`, `PhotoURL`, `SyncStatus`.

---

## 3. 前端流程與實作 (app.js)

### 3.1 登入/切換旅程流程 (Login Flow)

#### 邏輯判定
*   **輸入**：使用者輸入 Tripcode。
*   **檢查網路狀態**：若離線 (`!navigator.onLine`)，禁止登入新 Tripcode，提示「需連網以同步資料」。
*   **呼叫 API** `getTripInfo(tripcode)`：
    *   回傳：`isValid`, `tripStatus`, `existingMembers` (List of names), `employeeList` (List for dropdown).

#### 身份確認 UI
*   **顯示輸入框**：MemberName。
*   **前端驗證**：比對 `existingMembers`。
    *   **若名稱已存在**：提示「名稱已存在，是否為恢復身份？」，若是，進入恢復流程；若否，要求改名。
    *   **若名稱不存在**：進入新成員註冊流程。

#### 員工綁定 (Employee Binding)
*   **自動模式**：若登入 Email (透過 Google Auth 或參數) 在 `employeeList` 中找到，自動選定。
*   **手動模式**：顯示 Employee 下拉選單 (顯示 姓名 + 部門)。
*   **送出註冊**：呼叫 API `joinTrip(tripcode, memberName, employeeId)`。

### 3.2 裝置重置與同步 (Reset & Sync)

#### 觸發時機
*   `joinTrip` 成功回傳後。

#### 執行動作
1.  **清空 LocalStorage/IndexedDB**：清除舊 Tripcode 的所有暫存資料。
2.  **寫入新 Session**：
    *   `localStorage.setItem('currentTripcode', tripcode)`
    *   `localStorage.setItem('currentMemberName', memberName)`
    *   `localStorage.setItem('currentEmployeeID', employeeId)`
3.  **下載雲端資料**：
    *   接收後端回傳的該 Tripcode 所有 Expenses 與 TripItems。
    *   寫入 IndexedDB。
4.  **標記同步完成**：設定 `lastSyncTime`，解鎖離線操作功能。

---

## 4. 後端邏輯實作 (Code.gs)

### 4.1 關鍵函數定義

#### `function getTripInfo(tripcode)`
*   **目的**：登入前檢查與選單準備。
*   **邏輯**：
    1.  查詢 `Trips` 表確認 Tripcode 有效性。
    2.  查詢 `TripMembers` 表，篩選出該 Tripcode 的所有 `MemberName`。
    3.  查詢 `Employees` 表，回傳精簡名單 (ID, Name, Email) 供前端選單使用。
    4.  回傳 JSON。

#### `function joinTrip(tripcode, memberName, employeeId, email)`
*   **目的**：註冊成員並回傳初始資料。
*   **邏輯**：
    1.  使用 `LockService` 避免並發寫入衝突。
    2.  **重複性檢查**：
        *   檢查 `TripMembers` 中是否已有 `{Tripcode, MemberName}`。
        *   若有且 `EmployeeID` 符合 -> 視為 **恢復裝置 (Recovery)**。
        *   若有但 `EmployeeID` 不符 -> 報錯「名稱已被他人使用」。
        *   若無 -> 寫入新的一列至 `TripMembers`。
    3.  **資料提取**：
        *   撈取該 Tripcode 下的所有 Expenses。
        *   撈取該 Tripcode 下的所有 TripItems (公費項目)。
    4.  **回傳**：`{ success: true, expenses: [...], tripItems: [...] }`。

#### `function syncData(data)` (上傳資料)
*   **調整**：接收前端上傳的單據時，單據物件內必須包含 `EmployeeID`。
*   **邏輯**：寫入 `Expenses` 工作表時，直接寫入前端傳來的 `EmployeeID`，不需後端再次查表 (確保離線時前端邏輯的一致性)。

---

## 5. 安全性與防護 (Security)

### MemberName 唯一性
*   **Scope**：僅在 `Tripcode` 內唯一。
*   **實作**：後端 `joinTrip` 使用 `ScriptLock`，先讀後寫 (Read-Check-Write) 確保原子性 (Atomicity)。

### EmployeeID 冒用
*   **機制**：系統不強制驗證，由團長 (Leader) 在後台/報表審核時人工核實。
*   **輔助**：`TripMembers` 表記錄 `JoinDate` 與 `DeviceID` (若有)，方便追溯。

---

## 6. 離線操作策略

### 狀態判斷
*   App 啟動時檢查 `localStorage.getItem('currentTripcode')`。
*   **若無值**：強制留在登入頁，且若無網路則顯示「請連線以登入」。
*   **若有值**：進入主畫面，讀取 IndexedDB 顯示資料。

### 新增單據
1.  讀取 `localStorage` 中的 `currentMemberName` 與 `currentEmployeeID` 自動填入單據。
2.  儲存至 IndexedDB，標記 `sync: 0`。

### 背景同步
*   偵測到網路恢復 (`window.addEventListener('online')`) 時，觸發上傳。