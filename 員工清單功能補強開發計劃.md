員工清單功能補強 - 實作計畫
概要
依據「員工清單使用情境說明與調整.md」需求，在現有系統上增加：

EmployeesMaster 工作表 — 管理員在 Google Sheets 手動維護的員工主檔
TripMembers 工作表 — 旅程成員關聯表，記錄 MemberName ↔ EmployeeID 對應
後端 API — getTripInfo、joinTrip 兩支新 API
前端員工綁定步驟 — 在現有加入旅程流程後增加員工綁定
Admin 員工清單頁面增強 — 使用 EmployeeID 做跨旅程統計

使用者決策

EmployeesMaster 由管理員在 Google Sheets 手動建立，系統只做讀取
保留現有登入流程（TripCode → 姓名 → 角色），在之後加上員工綁定步驟
保持現有離線策略，僅在首次加入新 TripCode 時強制連線


階段 1：後端 — 資料庫架構變更
1.1 修改 initializeSheets() 新增工作表
檔案: gas/Code.gs (line 26-58)
在 sheetConfigs 陣列中新增兩個工作表定義：
javascript// EmployeesMaster — 員工主檔（管理員手動維護）
{
  name: 'EmployeesMaster',
  headers: [
    'EmployeeID',    // 主鍵 (如 EMP-0001)
    'Name',
    'Email',         // Unique, 用於自動比對
    'Department',
    'MonthlyLimit',  // 補助上限
    'UsedAmount',    // 已用額度
    'IsActive'       // 是否在職
  ]
}

// TripMembers — 旅程成員關聯表
{
  name: 'TripMembers',
  headers: [
    'Tripcode',      // Key 1
    'MemberName',    // Key 2 (Trip 內唯一)
    'EmployeeID',    // FK → EmployeesMaster
    'Role',          // 'Leader' | 'Member'
    'Status',        // 'Active' | 'Inactive'
    'JoinDate'       // ISO timestamp
  ]
}
1.2 Expenses 表新增 EmployeeID 欄位
在 initializeSheets() 的 Expenses headers 末尾新增 'employeeID'（第 20 欄，index 19）。

階段 2：後端 — 新增 API 函式
2.1 新增 handleGetTripInfo(data)
檔案: gas/Code.gs
位置: 在 handleGetMembers 之後 (line ~1719)
功能：

驗證 Tripcode 是否存在
從 TripMembers 取得該 Trip 的現有成員列表
Fallback: 從 Trips.members 欄位合併（向下相容）
從 EmployeesMaster 取得 IsActive 的員工列表供下拉選單
回傳 { success, isValid, tripStatus, leaderName, existingMembers, employeeList }

2.2 新增 handleJoinTrip(data)
檔案: gas/Code.gs
位置: 在 handleGetTripInfo 之後
功能：

使用 LockService.getScriptLock() 防止並發
接收參數: { tripcode, memberName, employeeId, role }
檢查 TripMembers 中 {Tripcode, MemberName} 是否已存在

存在且 EmployeeID 相同 → Recovery（恢復裝置）
存在且 EmployeeID 不同 → 報錯「名稱已被他人使用」
不存在 → 寫入新列到 TripMembers


同時更新 Trips.members CSV（向下相容）
呼叫既有 handleDownloadTrip() 取得該旅程資料
回傳 { success, isRecovery, tripInfo, expenses, employees, photos, serverLastModified }

2.3 更新 doPost() 路由
檔案: gas/Code.gs (line 127-177)
新增兩個 case：
javascriptcase 'getTripInfo':
  return jsonResponse(handleGetTripInfo(data));
case 'joinTrip':
  return jsonResponse(handleJoinTrip(data));
2.4 更新 handleAdminGetTripDetail()
檔案: gas/Code.gs (line 764-863)
在取得 employees 之後，增加讀取 TripMembers 的邏輯：

讀取 TripMembers 表中該 tripCode 的所有成員
讀取 EmployeesMaster 表以取得完整員工資訊
在回傳中新增 tripMembers 陣列和增強的 employees 資料（含 EmployeeID、Email、MonthlyLimit 等）

2.5 更新 handleSubmitTrip() 寫入 EmployeeID
檔案: gas/Code.gs (line 245)
寫入 Expenses 時，將前端傳來的 employeeID 寫入第 20 欄（新增欄位）。同時更新或建立 TripMembers 記錄。
2.6 更新 handleDownloadTrip() 回傳 TripMembers
檔案: gas/Code.gs (line ~990)
在回傳結果中增加 tripMembers 資料，讓前端知道 MemberName ↔ EmployeeID 的對應。

階段 3：API Client 更新
檔案: shared/api-client.js (在 line 293 之後)
新增兩個方法：
javascriptasync getTripInfo(tripcode) {
  return this._post({ action: 'getTripInfo', tripcode });
}

async joinTrip(tripcode, memberName, employeeId, role) {
  return this._post({
    action: 'joinTrip',
    tripcode, memberName, employeeId,
    role: role || 'Member'
  });
}

階段 4：前端 — 員工綁定流程
4.1 更新 appData 結構
檔案: app.js (line 142-169)
新增欄位：
javascriptcurrentEmployeeID: null,   // 綁定的員工 ID
employeeBinding: null,     // { employeeId, name, department }
4.2 新增 HTML 員工綁定區塊
檔案: index.html
在 Onboarding 流程中，在現有的 TripCode/角色選擇步驟之後，新增員工綁定 section：
html<div id="onboardingEmployeeBindingSection" class="hidden space-y-4">
  <h3>選擇對應員工</h3>
  <select id="employeeBindingSelect">
    <option value="">-- 請選擇對應員工 --</option>
  </select>
  <p class="text-xs text-gray-400">可用 Email 或姓名快速篩選</p>
  <div class="flex gap-3">
    <button onclick="skipEmployeeBinding()">略過</button>
    <button onclick="confirmEmployeeBinding()">確認綁定</button>
  </div>
</div>
4.3 修改 confirmJoinTrip() 流程
檔案: app.js (line 689-793)
在現有加入旅程成功之後：

呼叫 getTripInfo(tripCode) 取得員工清單
如果有 EmployeesMaster 資料 → 顯示員工綁定 UI
嘗試自動比對（Email 或姓名相符）
使用者確認後呼叫 joinTrip() 完成綁定
將 employeeId 存入 appData.currentEmployeeID 和 localStorage

4.4 費用提交時包含 EmployeeID
檔案: app.js
在 uploadToCloud() 或 submitTrip() 時，將 appData.currentEmployeeID 寫入每筆 expense 資料中。

階段 5：Admin 後台 — 員工清單頁面增強
5.1 更新 loadAllEmployeeData()
檔案: admin/admin.js (line 1500-1608)
修改為：

從 adminGetTripDetail() 回傳的增強資料（含 TripMembers + EmployeesMaster）建立員工彙總
使用 EmployeeID 做跨旅程統計（同一員工不同旅程的補助累計）
增加 employeeId、email、monthlyLimit、usedAmount 等欄位

5.2 更新 renderEmployeeList()
檔案: admin/admin.js (line 1616-1680)
增強表格：

新增「對應員工」欄位（顯示 EmployeeID 綁定的員工姓名）
新增「成員名稱」欄位（顯示 MemberName）
保留現有的統計欄位（申請狀態、補助狀態、補助金額試算、核銷金額）

5.3 更新 HTML 表格
檔案: admin/index.html (line 296-307)
更新 <thead> 加入新欄位，並將 colspan 從 7 調整為 8。

階段 6：資料遷移
6.1 遷移腳本
檔案: gas/Code.gs
新增 migrateToNewEmployeeSchema() 函式（在 Apps Script 編輯器中手動執行）：

從現有 Employees 表收集所有不重複的員工名稱 → 寫入 EmployeesMaster
從 Trips.members 欄位 → 生成 TripMembers 記錄
嘗試用姓名比對 EmployeeID
不修改現有的 Employees 表（向下相容）


修改檔案清單
檔案修改內容gas/Code.gs新增 EmployeesMaster/TripMembers 工作表、getTripInfo/joinTrip API、更新 adminGetTripDetail/submitTrip/downloadTripshared/api-client.js新增 getTripInfo() 和 joinTrip() 方法app.jsappData 新增欄位、confirmJoinTrip 流程加入員工綁定、費用提交含 EmployeeIDindex.html新增 onboardingEmployeeBindingSection HTMLadmin/admin.jsloadAllEmployeeData/renderEmployeeList 使用新 schemaadmin/index.html員工清單表格 thead 增加欄位
向下相容策略

現有 Employees 表保留不動 — 新系統同時寫入 TripMembers 和舊 Employees 表
Trips.members 欄位持續維護 — joinTrip 會同步更新 CSV
EmployeeID 為空的容錯 — 統計函式 fallback 到 employeeName 比對
舊版 App 相容 — 不帶 employeeId 的請求仍可正常運作
員工綁定可略過 — 使用者可選擇不綁定（backwards compatible）

驗證方式

在 Apps Script 編輯器執行 initializeSheets() 確認新工作表建立成功
手動在 EmployeesMaster 工作表新增幾筆測試員工資料
執行 migrateToNewEmployeeSchema() 確認舊資料遷移正確
在前端 App 測試加入旅程流程，確認員工綁定 UI 正常顯示
在 Admin 後台確認員工清單頁面能正確顯示跨旅程統計
確認現有的費用提交、同步、審核流程不受影響